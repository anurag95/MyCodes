#include<bits/stdc++.h>
using namespace std;
#define ll long long 
#define mod 1000000007
#define pb push_back
#define si(x) scanf("%d", &x);
#define sii(x,y) scanf("%d%d", &x, &y);
#define sll(x) scanf("%lld", &x);
#define pi(x) printf("%d\n", x);
#define pll(x) printf("%lld\n", x);
int main()
{
	int t, n, m, mouse[132000], hole[132000];
	si(t)
	while(t--)
	{
		sii(n, m)
		for(int i=0;i<n;i++)
			si(mouse[i])
		for(int i=0;i<m;i++)
			si(hole[i])
		sort(mouse, mouse+n);
		sort(hole, hole+m);
		for()

	}
	return 0;
}

